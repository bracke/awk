# Command-Line Reference

Supported options: `-F sep`, `-Fsep`, `-v name=value`, `-vname=value`, `-f file`, `-ffile`, `--help`, `--version`, `--color=auto|always|never`, and `--`.

Runtime assignment operands are recognized only when the name before `=` matches `[A-Za-z_][A-Za-z0-9_]*`.
