package Awk_Workflow_Install is
   --  Installed executable boundary validation for workflow gates.

   procedure Boundary;
   --  Install into a temporary prefix and validate installed executable metadata.
end Awk_Workflow_Install;
