package Awk_Workflow_Metadata is
   --  Alire and release metadata validation gates.

   procedure Run;
   --  Validate crate metadata, dependency constraints, pins, and generated config.
end Awk_Workflow_Metadata;
